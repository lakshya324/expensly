{
  "name": "expensly",
  "version": "2.1.0",
  "private": true,
  "description": "Expensly monorepo orchestration",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "check": "npm run typecheck && npm run lint && npm run test && npm run build",
    "audit:ci": "npm audit --prefix backend --omit=dev --audit-level=high && npm audit --prefix frontend --omit=dev --audit-level=high"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}
