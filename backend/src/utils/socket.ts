// Room management is handled natively by socket.io.
// Use `socket.join(roomId)` in connection handler and
// `getIO().to(roomId).emit(event, data)` in controllers.
// See: websocket/wsServer.ts for getIO()
